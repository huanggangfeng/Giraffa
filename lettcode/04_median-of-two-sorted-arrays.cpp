#include <vector>
#include <iostream>

using namespace std;

double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
    int total_size  = nums1.size() + num2.size();
    if(nums1)
    if (total_size % 2 == 0)
    {
        int median1, median2;
        int index1 = 0, index2 = 0;
        while((index1 + index2) <= (total_size/2+1))
        {
            if()

        }
    }
    else
    {

    }
}

int main()
{
    
    return 0;
}
